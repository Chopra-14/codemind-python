T = int(input())

for _ in range(T):
    N = int(input())
    A = list(map(int, input().split()))
    total_breaks = 0
    for length in A:
        total_breaks += (length - 1)
    print(total_breaks)
