 T= int(input())

for _ in range(T):
    N = int(input())
    sticks = list(map(int, input().split()))
    
    breaks = 0
    for length in sticks:
        if length > 1:
            breaks += (length - 1)
    
    print(breaks)
